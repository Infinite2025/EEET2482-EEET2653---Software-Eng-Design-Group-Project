#include<iostream>
#include<string>
#include<iomanip>
#include "Guest.h"
using namespace std;

Guest::Guest() {
    int creditPoints = 10; // Initialize credit points for guest
    int rating = -1; // Initialize rating for guest
}